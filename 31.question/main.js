var users = ['kinza', 'masooma', 'zainab', 'hina', 'admin'];
if (users.length === 0) {
    console.log("we need to fine some users !");
}
else {
    users = [];
    console.log("All used have been removed" + users.length);
}
